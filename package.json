{
  "name": "json-server-api",
  "version": "1.0.0",
  "description": "Fake API using json-server",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "server": "json-server server/db.json --port 4001"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
